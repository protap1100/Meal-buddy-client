import SectionTitle from "../Shared/SectionTitle";

const Membership = () => {
    return (
        <div className="mt-10">
            <SectionTitle heading='Membership' subHeading='Upgrade Your Membership for Better Meals'  ></SectionTitle>
        </div>
    );
};

export default Membership;